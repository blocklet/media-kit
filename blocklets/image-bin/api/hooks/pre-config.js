// const config = require('@blocklet/sdk/lib/config');

(() => {
  // eslint-disable-next-line no-console
  // console.info('trigger pre-config', config.env.preferences.extsInput);

  // if (config.env.preferences.extsInput?.includes('*')) {
  //   process.exit(1);
  //   return;
  // }

  process.exit(0);
})();
